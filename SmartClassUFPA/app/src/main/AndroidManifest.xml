<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="engcomp.smartclassufpa">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".BaseActivity"></activity>
        <activity
            android:name=".InicioActivity"
            android:label="SmartClass UFPA"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".FotosActivity"
            android:label="Fotos"
            android:theme="@style/AppTheme">

        </activity>

        <activity
            android:name=".HorariosActivity"
            android:label="Horários"
            android:theme="@style/AppTheme">

        </activity>

        <activity
            android:name=".TrabProvActivity"
            android:label="Trabalhos e Provas"
            android:theme="@style/AppTheme">

        </activity>

        <activity
            android:name=".ConfigActivity"
            android:label="Configurações"
            android:theme="@style/AppTheme">

        </activity>

        <activity
            android:name=".SobreActivity"
            android:label="Sobre"
            android:theme="@style/AppTheme">

        </activity>

        <activity
            android:name=".EmentaActivity"
            android:label="Ementa"
            android:theme="@style/AppTheme">

        </activity>

    </application>

</manifest>